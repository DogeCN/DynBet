# 2026-07-30

## 完成任务：补全 example.ipynb 教程 Notebook

- DynBet 是下注模拟框架（Game / Vault / Session / Strategy / Data）。原有 example.ipynb 是 QuickStart 教程，断在 3.4.1（代码不可运行：`Game([True,False])` 未实现 `once`、`Peak()` 缺参数）。
- 补全内容：保留 1–3.3 原样；修复 3.4.1（Fork + Peak/Lowest 可运行示例）；新增 3.4.2 Lock；新增 **Section 4 渐进式策略**（Martingale/DAlembert/Fibonacci/Labouchere/Stepping，用确定性 SimpleGame 演示下注额变化）；新增 **Section 5 matplotlib 可视化**（余额曲线对比 / 蒙特卡洛最终余额分布 / Martingale 回撤）。
- 新增 `Coin` 公平硬币游戏（p=0.5，期望收益=0）用于有输有赢的演示。
- 关键坑：
  - matplotlib 3.11 中 `plt.boxplot(labels=...)` 已改名为 `tick_labels=`。
  - `%matplotlib inline` 是 Jupyter magic，不能在纯 `exec` 中校验（需在真实 kernel 里跑）。
  - 渐进式策略破产（money→0）后 `Vault.snapshot()` 会抛 ValueError —— 仿真循环需 `if v.money<=0: 平躺` 兜底。
  - `Progressive` 每局从 `Data` 历史重算状态，所以工厂函数每次新建实例也能正确累积状态。
- 生成/校验工具链保留：`build_nb.py`（用 nbformat 生成 notebook）、`_exec_nb.py`（用 nbclient 执行并内嵌图形）。
- 已用 nbclient 实际执行 notebook，3 张图已内嵌，39 个 cell 全部跑通。

## 追加：修复 matplotlib 中文显示（Glyph missing 警告）
- 症状：图表中文变方块 + `UserWarning: Glyph ... missing from font(s) DejaVu Sans`。
- 修复：在 Section 5 绘图首单元格加中文字体自动配置 —— `plt.rcParams["font.sans-serif"]=[<首个可用中文字体>,"DejaVu Sans"]` + `plt.rcParams["axes.unicode_minus"]=False`。本机选到 `Microsoft YaHei`。
- 兜底字体列表：Microsoft YaHei / SimHei / SimSun / KaiTi / STKaiti / STSong / Noto Sans CJK SC / WenQuanYi Zen Hei / Source Han Sans SC（跨机器无雅黑也能降级）。
- 重新 build + 执行，嵌入图中文正常，glyph 警告 0 条。
